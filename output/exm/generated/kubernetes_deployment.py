from __future__ import annotations

"""
Auto-generated by JsonSchemaCodeGen v1.2.0
Generated: 2026-01-29T14:57:05.104272

Copyright (C) 2025-2030, Ashutosh Sinha (ajsinha@gmail.com)
DO NOT EDIT - Changes will be overwritten.
"""
# Generated from: 14_kubernetes_deployment.json

"""
Auto-generated Python classes from JSON Schema.

Generated by JsonSchemaCodeGen on 2026-01-29 14:57:05

This module contains dataclasses generated from JSON Schema definitions.
Each class includes:
- Type hints for all properties
- Serialization methods (to_dict, to_json)
- Deserialization class methods (from_dict, from_json)
- Validation method to check required fields and enum values
"""


import json
from dataclasses import dataclass, field, asdict
from typing import Any, Dict, List, Optional, Union
from datetime import datetime, date
from typing import Dict, Any
from typing import List


@dataclass
class ValidationResult:
    """Result of validation containing status and any errors."""
    is_valid: bool
    errors: List[str] = field(default_factory=list)
    
    def __str__(self) -> str:
        if self.is_valid:
            return "Valid"
        return f"Invalid: {'; '.join(self.errors)}"
    
    def __bool__(self) -> bool:
        return self.is_valid

@dataclass
class LabelSelector:
    """Generated class for LabelSelector."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "matchLabels": "matchLabels",
            "matchExpressions": "matchExpressions"
        }

    matchLabels: Dict[str, Any] = field(default_factory=dict)
    matchExpressions: List["AnonymousObject"] = field(default_factory=list)

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'LabelSelector':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'LabelSelector':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return []

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Container:
    """Generated class for Container."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "name": "name",
            "image": "image",
            "imagePullPolicy": "imagePullPolicy",
            "command": "command",
            "args": "args",
            "env": "env",
            "ports": "ports",
            "resources": "resources",
            "volumeMounts": "volumeMounts",
            "livenessProbe": "livenessProbe",
            "readinessProbe": "readinessProbe"
        }

    name: Optional[str] = None
    image: Optional[str] = None
    imagePullPolicy: Optional["ImagepullpolicyEnum"] = None
    command: List[str] = field(default_factory=list)
    args: List[str] = field(default_factory=list)
    env: List["AnonymousObject"] = field(default_factory=list)
    ports: List["AnonymousObject"] = field(default_factory=list)
    resources: Optional["Resources"] = None
    volumeMounts: List["AnonymousObject"] = field(default_factory=list)
    livenessProbe: Optional["Livenessprobe"] = None
    readinessProbe: Optional["Readinessprobe"] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Container':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Container':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['name', 'image']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {'imagePullPolicy': ['Always', 'Never', 'IfNotPresent']}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class EnvVar:
    """Generated class for EnvVar."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "name": "name",
            "value": "value",
            "valueFrom": "valueFrom"
        }

    name: Optional[str] = None
    value: Optional[str] = None
    valueFrom: Optional["Valuefrom"] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'EnvVar':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'EnvVar':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['name']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class KeyRef:
    """Generated class for KeyRef."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "name": "name",
            "key": "key"
        }

    name: Optional[str] = None
    key: Optional[str] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'KeyRef':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'KeyRef':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['name', 'key']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class ContainerPort:
    """Generated class for ContainerPort."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "containerPort": "containerPort",
            "name": "name",
            "protocol": "protocol"
        }

    containerPort: Optional[int] = None
    name: Optional[str] = None
    protocol: Optional["ProtocolEnum"] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'ContainerPort':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'ContainerPort':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['containerPort']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {'protocol': ['TCP', 'UDP']}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class ResourceRequirements:
    """Generated class for ResourceRequirements."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "limits": "limits",
            "requests": "requests"
        }

    limits: Dict[str, Any] = field(default_factory=dict)
    requests: Dict[str, Any] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'ResourceRequirements':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'ResourceRequirements':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return []

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class VolumeMount:
    """Generated class for VolumeMount."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "name": "name",
            "mountPath": "mountPath",
            "readOnly": "readOnly",
            "subPath": "subPath"
        }

    name: Optional[str] = None
    mountPath: Optional[str] = None
    readOnly: Optional[bool] = None
    subPath: Optional[str] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'VolumeMount':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'VolumeMount':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['name', 'mountPath']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Probe:
    """Generated class for Probe."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "httpGet": "httpGet",
            "tcpSocket": "tcpSocket",
            "initialDelaySeconds": "initialDelaySeconds",
            "periodSeconds": "periodSeconds",
            "timeoutSeconds": "timeoutSeconds",
            "failureThreshold": "failureThreshold"
        }

    httpGet: Optional["Httpget"] = None
    tcpSocket: Optional["Tcpsocket"] = None
    initialDelaySeconds: Optional[int] = None
    periodSeconds: Optional[int] = None
    timeoutSeconds: Optional[int] = None
    failureThreshold: Optional[int] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Probe':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Probe':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return []

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Volume:
    """Generated class for Volume."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "name": "name",
            "configMap": "configMap",
            "secret": "secret",
            "persistentVolumeClaim": "persistentVolumeClaim",
            "emptyDir": "emptyDir"
        }

    name: Optional[str] = None
    configMap: Optional["Configmap"] = None
    secret: Optional["Secret"] = None
    persistentVolumeClaim: Optional["Persistentvolumeclaim"] = None
    emptyDir: Dict[str, Any] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Volume':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Volume':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['name']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class KubernetesDeployment:
    """Kubernetes Deployment specification"""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "apiVersion": "apiVersion",
            "kind": "kind",
            "metadata": "metadata",
            "spec": "spec"
        }

    apiVersion: Optional[str] = None
    kind: Optional[str] = None
    metadata: Optional["Metadata"] = None
    spec: Optional["Spec"] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'KubernetesDeployment':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'KubernetesDeployment':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['apiVersion', 'kind', 'metadata', 'spec']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)
