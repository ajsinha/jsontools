from __future__ import annotations

"""
Auto-generated by JsonSchemaCodeGen v1.2.0
Generated: 2026-01-29T14:57:05.115325

Copyright (C) 2025-2030, Ashutosh Sinha (ajsinha@gmail.com)
DO NOT EDIT - Changes will be overwritten.
"""
# Generated from: 21_game_entity.json

"""
Auto-generated Python classes from JSON Schema.

Generated by JsonSchemaCodeGen on 2026-01-29 14:57:05

This module contains dataclasses generated from JSON Schema definitions.
Each class includes:
- Type hints for all properties
- Serialization methods (to_dict, to_json)
- Deserialization class methods (from_dict, from_json)
- Validation method to check required fields and enum values
"""


import json
from dataclasses import dataclass, field, asdict
from typing import Any, Dict, List, Optional, Union
from datetime import datetime, date
from typing import Dict, Any
from typing import List
from uuid import UUID


@dataclass
class ValidationResult:
    """Result of validation containing status and any errors."""
    is_valid: bool
    errors: List[str] = field(default_factory=list)
    
    def __str__(self) -> str:
        if self.is_valid:
            return "Valid"
        return f"Invalid: {'; '.join(self.errors)}"
    
    def __bool__(self) -> bool:
        return self.is_valid

@dataclass
class Transform:
    """Generated class for Transform."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "position": "position",
            "rotation": "rotation",
            "scale": "scale"
        }

    position: Optional["Position"] = None
    rotation: Optional["Rotation"] = None
    scale: Optional["Scale"] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Transform':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Transform':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return []

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Vector3:
    """Generated class for Vector3."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "x": "x",
            "y": "y",
            "z": "z"
        }

    x: Optional[float] = 0
    y: Optional[float] = 0
    z: Optional[float] = 0

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Vector3':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Vector3':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['x', 'y', 'z']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Quaternion:
    """Generated class for Quaternion."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "x": "x",
            "y": "y",
            "z": "z",
            "w": "w"
        }

    x: Optional[float] = 0
    y: Optional[float] = 0
    z: Optional[float] = 0
    w: Optional[float] = 1

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Quaternion':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Quaternion':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['x', 'y', 'z', 'w']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Component:
    """Generated class for Component."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "type_": "type",
            "enabled": "enabled",
            "properties": "properties"
        }

    type_: Optional[str] = None
    enabled: Optional[bool] = True
    properties: Dict[str, Any] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Component':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Component':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['type_']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Script:
    """Generated class for Script."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "name": "name",
            "class_": "class",
            "parameters": "parameters"
        }

    name: Optional[str] = None
    class_: Optional[str] = None
    parameters: Dict[str, Any] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Script':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Script':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['name', 'class_']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class Animation:
    """Generated class for Animation."""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "controller": "controller",
            "clips": "clips"
        }

    controller: Optional[str] = None
    clips: List["AnonymousObject"] = field(default_factory=list)

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'Animation':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'Animation':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return []

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)

@dataclass
class GameEntity:
    """Game entity with components and behaviors"""

    # Property name mapping for JSON serialization
    @classmethod
    def _get_json_mapping(cls) -> Dict[str, str]:
        return {
            "id_": "id",
            "name": "name",
            "transform": "transform",
            "tag": "tag",
            "layer": "layer",
            "active": "active",
            "static": "static",
            "components": "components",
            "colliders": "colliders",
            "rigidBody": "rigidBody",
            "renderer": "renderer",
            "scripts": "scripts",
            "animation": "animation",
            "children": "children",
            "prefab": "prefab"
        }

    id_: Optional[UUID] = None
    name: Optional[str] = None
    transform: Optional["Transform"] = None
    tag: Optional[str] = None
    layer: Optional[int] = None
    active: Optional[bool] = True
    static: Optional[bool] = False
    components: List["AnonymousObject"] = field(default_factory=list)
    colliders: List["AnonymousObject"] = field(default_factory=list)
    rigidBody: Optional["AnonymousObject"] = None
    renderer: Optional["AnonymousObject"] = None
    scripts: List["AnonymousObject"] = field(default_factory=list)
    animation: Optional["Animation"] = None
    children: List["Gameentity"] = field(default_factory=list)
    prefab: Optional[str] = None

    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary with original JSON property names."""
        result = {}
        for py_name, json_name in self._get_json_mapping().items():
            value = getattr(self, py_name, None)
            if value is not None:
                if hasattr(value, 'to_dict'):
                    result[json_name] = value.to_dict()
                elif isinstance(value, list):
                    result[json_name] = [
                        item.to_dict() if hasattr(item, 'to_dict') else item
                        for item in value
                    ]
                elif isinstance(value, (datetime, date)):
                    result[json_name] = value.isoformat()
                else:
                    result[json_name] = value
        return result

    def to_json(self, indent: int = 2) -> str:
        """Serialize to JSON string."""
        return json.dumps(self.to_dict(), indent=indent, default=str)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'GameEntity':
        """Create instance from dictionary."""
        reverse_mapping = {v: k for k, v in cls._get_json_mapping().items()}
        kwargs = {}
        for json_name, value in data.items():
            py_name = reverse_mapping.get(json_name, json_name)
            if py_name in cls.__annotations__:
                kwargs[py_name] = value
        return cls(**kwargs)

    @classmethod
    def from_json(cls, json_str: str) -> 'GameEntity':
        """Create instance from JSON string."""
        return cls.from_dict(json.loads(json_str))

    @classmethod
    def _get_required_fields(cls) -> List[str]:
        """Get list of required field names."""
        return ['id_', 'name', 'transform']

    @classmethod
    def _get_enum_constraints(cls) -> Dict[str, List[Any]]:
        """Get enum constraints for fields."""
        return {}

    def validate(self) -> 'ValidationResult':
        """
        Validate that all required fields are populated and enum values are valid.
        
        Returns:
            ValidationResult with is_valid flag and list of errors
        """
        errors = []
        
        # Check required fields
        for field_name in self._get_required_fields():
            value = getattr(self, field_name, None)
            if value is None:
                errors.append(f"Required field '{field_name}' is not set")
        
        # Check enum constraints
        for field_name, valid_values in self._get_enum_constraints().items():
            value = getattr(self, field_name, None)
            if value is not None and value not in valid_values:
                errors.append(f"Field '{field_name}' has invalid value '{value}'. Must be one of: {valid_values}")
        
        return ValidationResult(is_valid=len(errors) == 0, errors=errors)
